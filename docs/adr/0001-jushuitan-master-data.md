# 聚水潭作为主数据源头

本系统的商品（产品与规格）主数据以聚水潭为唯一可信来源。本地数据库作为只读缓存，定时同步聚水潭数据。日常运营中，所有新建、编辑操作都在聚水潭完成，本系统仅提供查看、关联（BOM、采购单、库存查询）能力。

做这个决定的原因是：团队已经在聚水潭中维护完整的商品档案（SKU 编码、价格、图片、规格参数），如果在本系统再建一套编辑能力，会造成双写冲突和维护负担。本地只读缓存的策略避免了数据不一致问题，也让同步逻辑保持简单（单向、覆盖式）。
